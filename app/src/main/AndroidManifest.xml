<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patinho.logoali">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash screen -->
        <activity
            android:name=".GPlusActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/search_activity_label" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".ActivityDetalhe">
            <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
        </activity>
        <activity android:name=".ActivityEstabelecimentos"/>
        <activity android:name=".ActivityEditEstab"/>
        <activity android:name=".ActivityCreateEstab"/>
        <activity android:name=".QRReader"/>
        <activity android:name=".QRWriter"/>
    </application>

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.CAMERA"/>

</manifest>